`Sure! Let's build the **Adding page** step by step. The goal is to create a page where you can add new properties to your`propertiesData.json` file.

Since you're working with a static JSON file, the data won't automatically update unless you save it in the file system or use an API to handle updates. But I'll guide you on how to build the form and prepare the data for saving. For now, we'll just focus on **handling the form and adding the new property** to the state.

### Step 1: Create the Basic Form Layout

Let's first create a basic form where you can input the new property details (ID, street, city, postcode, tenant, rent, and status).

```javascript
import { useState } from "react";

function Adding() {
  // State for storing form inputs
  const [newProperty, setNewProperty] = useState({
    id: "",
    street: "",
    city: "",
    postcode: "",
    tenant: "",
    rent: "",
    status: "",
  });

  // Handle input changes
  const handleInputChange = (e) => {
    const { name, value } = e.target;
    setNewProperty({
      ...newProperty,
      [name]: value,
    });
  };

  // Handle form submission
  const handleSubmit = (e) => {
    e.preventDefault();
    // You will handle adding the new property here
    console.log(newProperty);
  };

  return (
    <div className="p-4">
      <h2 className="text-2xl font-bold mb-4">Add New Property</h2>
      <form onSubmit={handleSubmit}>
        <div className="mb-4">
          <label
            htmlFor="street"
            className="block text-sm font-medium text-gray-700"
          >
            Street Name
          </label>
          <input
            type="text"
            name="street"
            id="street"
            value={newProperty.street}
            onChange={handleInputChange}
            className="w-full p-2 border rounded-md"
            placeholder="Enter street name"
          />
        </div>
        {/* Add similar inputs for other properties like city, postcode, etc. */}
        <button type="submit" className="bg-blue-500 text-white p-2 rounded">
          Add Property
        </button>
      </form>
    </div>
  );
}

export default Adding;
```

### Breakdown of the Code:

- **State**: We use `useState` to store the form data (`newProperty`), which initially contains empty strings.
- **handleInputChange**: This function updates the respective state key when an input field changes.
- **handleSubmit**: This function will be triggered when the form is submitted. For now, it just logs the form data to the console.

Now, we have a basic form with an input field for the **street** of the property.

### Step 2: Add the Other Input Fields

Next, we'll add the remaining fields (city, postcode, tenant, rent, status) to the form.

```javascript
import { useState } from "react";

function Adding() {
  const [newProperty, setNewProperty] = useState({
    id: "",
    street: "",
    city: "",
    postcode: "",
    tenant: "",
    rent: "",
    status: "",
  });

  const handleInputChange = (e) => {
    const { name, value } = e.target;
    setNewProperty({
      ...newProperty,
      [name]: value,
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log(newProperty);
    // You will handle adding the new property here
  };

  return (
    <div className="p-4">
      <h2 className="text-2xl font-bold mb-4">Add New Property</h2>
      <form onSubmit={handleSubmit}>
        <div className="mb-4">
          <label
            htmlFor="street"
            className="block text-sm font-medium text-gray-700"
          >
            Street Name
          </label>
          <input
            type="text"
            name="street"
            id="street"
            value={newProperty.street}
            onChange={handleInputChange}
            className="w-full p-2 border rounded-md"
            placeholder="Enter street name"
          />
        </div>

        <div className="mb-4">
          <label
            htmlFor="city"
            className="block text-sm font-medium text-gray-700"
          >
            City
          </label>
          <input
            type="text"
            name="city"
            id="city"
            value={newProperty.city}
            onChange={handleInputChange}
            className="w-full p-2 border rounded-md"
            placeholder="Enter city"
          />
        </div>

        <div className="mb-4">
          <label
            htmlFor="postcode"
            className="block text-sm font-medium text-gray-700"
          >
            Postcode
          </label>
          <input
            type="text"
            name="postcode"
            id="postcode"
            value={newProperty.postcode}
            onChange={handleInputChange}
            className="w-full p-2 border rounded-md"
            placeholder="Enter postcode"
          />
        </div>

        <div className="mb-4">
          <label
            htmlFor="tenant"
            className="block text-sm font-medium text-gray-700"
          >
            Tenant
          </label>
          <input
            type="text"
            name="tenant"
            id="tenant"
            value={newProperty.tenant}
            onChange={handleInputChange}
            className="w-full p-2 border rounded-md"
            placeholder="Enter tenant name"
          />
        </div>

        <div className="mb-4">
          <label
            htmlFor="rent"
            className="block text-sm font-medium text-gray-700"
          >
            Rent (£)
          </label>
          <input
            type="number"
            name="rent"
            id="rent"
            value={newProperty.rent}
            onChange={handleInputChange}
            className="w-full p-2 border rounded-md"
            placeholder="Enter rent"
          />
        </div>

        <div className="mb-4">
          <label
            htmlFor="status"
            className="block text-sm font-medium text-gray-700"
          >
            Status
          </label>
          <select
            name="status"
            id="status"
            value={newProperty.status}
            onChange={handleInputChange}
            className="w-full p-2 border rounded-md"
          >
            <option value="">Select Status</option>
            <option value="Occupied">Occupied</option>
            <option value="Vacant">Vacant</option>
          </select>
        </div>

        <button type="submit" className="bg-blue-500 text-white p-2 rounded">
          Add Property
        </button>
      </form>
    </div>
  );
}

export default Adding;
```

### Explanation:

- We've added the remaining form fields for **city**, **postcode**, **tenant**, **rent**, and **status**.
- For rent, I used a `type="number"` input to make sure users enter a number.
- For status, I used a `select` dropdown with options for **Occupied** and **Vacant**.

### Step 3: Handle Form Submission

Now, we need to handle the form submission and add the new property to your JSON file. However, since we cannot directly modify a JSON file on the front-end (as it's static), we'll need to store the new property temporarily in the state and either save it in a backend or simply log it for now.

We'll log the new property to the console, but in a real-world scenario, you'd send the data to a back-end server, or locally store it in something like `localStorage` or a database.

```javascript
const handleSubmit = (e) => {
  e.preventDefault();

  // Add the new property to the list (you can also save it in localStorage or a server)
  console.log(newProperty);
  // Reset the form after submission
  setNewProperty({
    id: "",
    street: "",
    city: "",
    postcode: "",
    tenant: "",
    rent: "",
    status: "",
  });
};
```

### Step 4: Store Data Temporarily (Optional)

If you want to store the property temporarily in `localStorage` (for example), you can do something like this in the `handleSubmit` function:

```javascript
const handleSubmit = (e) => {
  e.preventDefault();

  // Save the new property to localStorage (if you want to keep the data across page reloads)
  const existingProperties =
    JSON.parse(localStorage.getItem("properties")) || [];
  existingProperties.push(newProperty);
  localStorage.setItem("properties", JSON.stringify(existingProperties));

  // Optionally reset form after submission
  setNewProperty({
    id: "",
    street: "",
    city: "",
    postcode: "",
    tenant: "",
    rent: "",
    status: "",
  });
};
```

### Step 5: Confirming the Additions

For now, after submitting, we just log the new property to the console. In the next steps, you can choose to update the displayed property list by pulling the updated data from your storage or server.

---

Let me know if you need further steps for saving to a backend or displaying the updated list immediately after adding!

`

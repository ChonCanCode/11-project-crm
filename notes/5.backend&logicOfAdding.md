1. **Target architecture (what you’re building)**
2. **Recommended beginner-friendly tech stack**
3. **Step-by-step backend build**
4. **Authentication (login & authorization)**
5. **Cloud database options (free)**
6. **How your React code will change**
7. **Next learning milestones**

---

## 1️⃣ What you’re building (big picture)

Right now:

```
React → local JSON file
```

What you want:

```
React (Frontend)
   ↓ HTTP requests
Backend API (Node.js / Express)
   ↓
Auth check (login / permissions)
   ↓
Cloud Database (properties)
```

The backend becomes the **middleman**:

- Verifies user identity
- Controls access
- Reads/writes data from the database

This is exactly how production apps work.

---

## 2️⃣ Recommended beginner-friendly stack

This stack is **free**, popular, and well-documented:

### Frontend (you already have)

- ✅ React
- ✅ React Router

### Backend

- **Node.js**
- **Express.js** (API framework)

### Authentication

- **JWT (JSON Web Tokens)**
- Password hashing with **bcrypt**

### Database (free tiers)

**Pick ONE of these (I recommend #1):**

| Database             | Type  | Free Tier | Notes              |
| -------------------- | ----- | --------- | ------------------ |
| **MongoDB Atlas** ⭐ | NoSQL | ✅ Yes    | Best for beginners |
| Supabase             | SQL   | ✅ Yes    | Firebase-like      |
| Firebase Firestore   | NoSQL | ✅ Yes    | Easy auth built-in |
| Neon / PlanetScale   | SQL   | ✅ Yes    | More advanced      |

👉 **I’ll teach using MongoDB Atlas** because it maps nicely to your JSON structure.

---

## 3️⃣ Step-by-step backend setup

### Step 1: Create backend folder

```
your-project/
  frontend/
  backend/
```

Inside `backend`:

```bash
npm init -y
npm install express cors mongoose dotenv bcrypt jsonwebtoken
```

---

### Step 2: Basic Express server

📁 `backend/server.js`

```js
const express = require("express");
const mongoose = require("mongoose");
const cors = require("cors");
require("dotenv").config();

const app = express();

app.use(cors());
app.use(express.json());

// test route
app.get("/", (req, res) => {
  res.send("API is running");
});

mongoose
  .connect(process.env.MONGO_URI)
  .then(() => console.log("MongoDB connected"))
  .catch((err) => console.error(err));

app.listen(5000, () => {
  console.log("Server running on port 5000");
});
```

---

### Step 3: Create MongoDB Atlas (FREE)

1. Go to **[https://www.mongodb.com/atlas](https://www.mongodb.com/atlas)**
2. Create free account
3. Create **FREE shared cluster**
4. Get your connection string
5. Create `.env` file:

```
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/propertydb
JWT_SECRET=supersecretkey
```

---

## 4️⃣ Property database (replacing JSON)

### Step 4: Property model (Schema)

📁 `backend/models/Property.js`

```js
const mongoose = require("mongoose");

const propertySchema = new mongoose.Schema({
  street: String,
  city: String,
  postcode: String,
  tenant: String,
  rent: Number,
  status: String,
});

module.exports = mongoose.model("Property", propertySchema);
```

---

### Step 5: Property API routes

📁 `backend/routes/properties.js`

```js
const express = require("express");
const Property = require("../models/Property");
const router = express.Router();

// GET all properties
router.get("/", async (req, res) => {
  const properties = await Property.find();
  res.json(properties);
});

// POST new property
router.post("/", async (req, res) => {
  const property = new Property(req.body);
  await property.save();
  res.status(201).json(property);
});

module.exports = router;
```

Register route in `server.js`:

```js
const propertyRoutes = require("./routes/properties");
app.use("/api/properties", propertyRoutes);
```

---

## 5️⃣ Authentication (login & authority)

### Step 6: User model

📁 `backend/models/User.js`

```js
const mongoose = require("mongoose");

const userSchema = new mongoose.Schema({
  email: String,
  password: String,
  role: { type: String, default: "user" },
});

module.exports = mongoose.model("User", userSchema);
```

---

### Step 7: Register & login routes

📁 `backend/routes/auth.js`

```js
const express = require("express");
const bcrypt = require("bcrypt");
const jwt = require("jsonwebtoken");
const User = require("../models/User");

const router = express.Router();

// Register
router.post("/register", async (req, res) => {
  const hashedPassword = await bcrypt.hash(req.body.password, 10);
  const user = new User({
    email: req.body.email,
    password: hashedPassword,
  });
  await user.save();
  res.status(201).send("User created");
});

// Login
router.post("/login", async (req, res) => {
  const user = await User.findOne({ email: req.body.email });
  if (!user) return res.status(401).send("Invalid credentials");

  const valid = await bcrypt.compare(req.body.password, user.password);
  if (!valid) return res.status(401).send("Invalid credentials");

  const token = jwt.sign({ id: user._id }, process.env.JWT_SECRET);
  res.json({ token });
});

module.exports = router;
```

Register in `server.js`:

```js
const authRoutes = require("./routes/auth");
app.use("/api/auth", authRoutes);
```

---

## 6️⃣ Protecting routes (authority check)

📁 `backend/middleware/authMiddleware.js`

```js
const jwt = require("jsonwebtoken");

module.exports = (req, res, next) => {
  const token = req.headers.authorization?.split(" ")[1];
  if (!token) return res.status(401).send("Access denied");

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch {
    res.status(401).send("Invalid token");
  }
};
```

Protect property routes:

```js
const auth = require("../middleware/authMiddleware");

router.get("/", auth, async (req, res) => {
  const properties = await Property.find();
  res.json(properties);
});
```

---

## 7️⃣ Update your React code (important part)

### Replace JSON import ❌

```js
import propertiesData from "../json/propertiesData.json";
```

### Fetch from backend ✅

```js
useEffect(() => {
  fetch("http://localhost:5000/api/properties", {
    headers: {
      Authorization: `Bearer ${localStorage.getItem("token")}`,
    },
  })
    .then((res) => res.json())
    .then((data) => setProperties(data));
}, []);
```

Now:

- Frontend → Backend
- Backend → Database
- Backend → Auth check

🎉 You’re officially full-stack.

---

## 8️⃣ Free database summary (recommended)

✅ **MongoDB Atlas** – best choice
✅ Supabase – great if you prefer SQL
✅ Firebase – easiest auth, less backend control

---

## 9️⃣ What to learn next (roadmap)

1. ✅ REST APIs
2. ✅ JWT authentication
3. ✅ Role-based access (admin vs user)
4. ⏭ Pagination & filtering
5. ⏭ Deploy backend (Render / Railway – free)
6. ⏭ Deploy frontend (Vercel / Netlify)

---

## Want me to continue?

I can:

- Walk you through **MongoDB Atlas setup visually**
- Help you **deploy this for free**
- Add **edit/delete properties**
- Add **admin-only access**

Just tell me 👍
